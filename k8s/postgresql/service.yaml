apiVersion: v1
kind: Service
metadata:
  name: meetloom-postgresql
spec:
  selector:
    app.kubernetes.io/name: meetloom-postgresql
  ports:
    - name: postgresql
      port: 5432
      targetPort: postgresql
