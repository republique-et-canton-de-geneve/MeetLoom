apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: meetloom
spec:
  to:
    kind: Service
    name: meetloom
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
